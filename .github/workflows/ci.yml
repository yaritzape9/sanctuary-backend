name: Backend CI

on:
  push:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: sanctuary
          POSTGRES_PASSWORD: sanctuary
          POSTGRES_DB: sanctuary
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: "maven"

      - name: Check config file locations
        run: |
          chmod +x scripts/check-config-location.sh
          ./scripts/check-config-location.sh

      - name: Run tests
        env:
          JWT_SECRET: ci-test-secret-do-not-use-in-prod
          SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/sanctuary
          SPRING_DATASOURCE_USERNAME: sanctuary
          SPRING_DATASOURCE_PASSWORD: sanctuary
        run: ./mvnw test